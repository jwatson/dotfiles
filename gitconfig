[user]
	name = John Watson
	email = john@photodojo.org
[alias]
        co = checkout
        cleanup = !git remote prune origin && git gc && git clean -df && git stash clear
[color]
        ui = auto
[color "branch"]
        current = yellow reverse
        local = yellow
        remote = green
[color "diff"]
        meta = yellow bold
        frag = magenta bold
        old = red bold
        new = green bold
[color "status"]
        added = yellow
        changed = green
        untracked = cyan
[branch]
        autosetupmerge = true
        autosetuprebase = always
[core]
	quotepath = false
	excludesfile = /Users/john/.gitignore
        editor = vim --noplugin
[push]
        default = current
[github]
        user = jwatson
